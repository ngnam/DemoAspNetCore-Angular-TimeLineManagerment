import { DatetimeSinceFormatPipe } from './datetime-since-format.pipe';

describe('DatetimeSinceFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DatetimeSinceFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
